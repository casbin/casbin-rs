table! {
    casbin_rules (id) {
        id -> Integer,
        ptype -> Nullable<Varchar>,
        v0 -> Nullable<Varchar>,
        v1 -> Nullable<Varchar>,
        v2 -> Nullable<Varchar>,
        v3 -> Nullable<Varchar>,
        v4 -> Nullable<Varchar>,
        v5 -> Nullable<Varchar>,
    }
}
